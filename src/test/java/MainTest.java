import org.junit.Test;
import static org.junit.Assert.*;

public class MainTest {
    @Test
    public void testMainMenu() {
        Main.mainMenu();
        assertTrue(true);  // Since mainMenu doesn't return a value, this is a placeholder assertion
    }
}
